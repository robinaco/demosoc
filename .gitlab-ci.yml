image: gradle:8.5-jdk17

stages:
  - build
  - test
  - sonar
  - quality_gate 

cache:
  paths:
    - .gradle/

before_script:
  - chmod +x gradlew

build:
  stage: build
  script:
    - ./gradlew clean compileJava --no-daemon

test:
  stage: test
  script:
    - ./gradlew test jacocoTestReport --no-daemon
  artifacts:
    reports:
      junit: build/test-results/test/*.xml

sonar:
  stage: sonar
  script:
    - chmod +x gradlew
    - echo "=== TODAS LAS VARIABLES CI/CD ==="
    - env | sort
    - echo "================================"
    - echo "URL=$SONAR_HOST_URL"
    - ./gradlew sonar --no-daemon -Dsonar.host.url=$SONAR_HOST_URL -Dsonar.organization=$SONAR_ORG -Dsonar.projectKey=$SONAR_PROJECT_KEY -Dsonar.token=$SONAR_TOKEN -Dsonar.coverage.jacoco.xmlReportPaths=build/reports/jacoco/test/jacocoTestReport.xml

    
 
quality_gate:
  stage: quality_gate
  image: curlimages/curl:latest
  before_script: []
  script:
    - echo "Consultando Quality Gate para el proyecto $SONAR_PROJECT_KEY en la rama $CI_COMMIT_BRANCH"
    
    # --- Verificar token (endpoint correcto para SonarCloud) ---
    - echo "Verificando autenticación con SonarCloud..."
    - |
      TOKEN_CHECK=$(curl -s -o /dev/null -w "%{http_code}" -u $SONAR_TOKEN: \
        "https://sonarcloud.io/api/authentication/validate")
      
      if [ "$TOKEN_CHECK" -ne 200 ]; then
        echo "❌ Error de autenticación con SonarCloud. Código HTTP: $TOKEN_CHECK"
        echo "Verifica que el SONAR_TOKEN sea válido."
        exit 1
      fi
      echo "✅ Autenticación exitosa."
    
    # --- Esperar a que SonarCloud procese el análisis ---
    - echo "Esperando a que SonarCloud procese el análisis..."
    - sleep 10
    
    # --- Consultar Quality Gate con reintentos ---
    - |
      for i in {1..12}; do
        echo "Intento $i: Consultando Quality Gate..."
        
        RESPONSE=$(curl -s -u $SONAR_TOKEN: \
          "https://sonarcloud.io/api/qualitygates/project_status?projectKey=$SONAR_PROJECT_KEY&branch=$CI_COMMIT_BRANCH")
        
        STATUS=$(echo "$RESPONSE" | grep -o '"status":"[^"]*"' | cut -d'"' -f4)
        
        echo "Estado actual: ${STATUS:-'No disponible'}"
        
        if [ "$STATUS" = "OK" ]; then
          echo "✅ Quality Gate PASSED"
          exit 0
        elif [ "$STATUS" = "ERROR" ]; then
          echo "❌ Quality Gate FAILED"
          echo "Respuesta completa: $RESPONSE"
          exit 1
        fi
        
        # Si no está listo, esperar 10 segundos
        sleep 10
      done
      
      echo "⏱️ Timeout: SonarCloud no completó el análisis a tiempo"
      echo "La rama '$CI_COMMIT_BRANCH' podría no haberse analizado correctamente"
      exit 1
      
  dependencies:
    - sonar
  only:
    - branches